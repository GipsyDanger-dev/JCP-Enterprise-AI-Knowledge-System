# Base URL backend API (tanpa trailing slash)
VITE_API_BASE_URL=http://localhost:8080/api

# Mock auth aktif secara default di development (tanpa backend).
# Set false saat backend sudah siap: VITE_USE_MOCK_AUTH=false
