# Core engine runs on the Python stdlib; everything here is optional.
# PDF parsing (optional — TXT/MD/DOCX work without any dependency).
pypdf>=4.0

# HTTP API (http_api.py) — required to serve the NestJS backend.
fastapi>=0.111
uvicorn>=0.30
pydantic>=2.7
# Multipart file uploads for POST /ingest-file (Backend streams the binary file).
python-multipart>=0.0.9
# Required by the current Starlette TestClient implementation used in AI tests.
httpx2>=0.1

# PostgreSQL + pgvector store (store.py) — required for the shared Data Layer.
psycopg[binary]>=3.1
pgvector>=0.2
